spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/easyshop
    username: postgres
    password: postgres1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create  # or "create" if you want to start fresh
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
